export interface OrderProduct {
  orderCode: string;
  productCode: string;
  totalPrice: number;
  orderDate: string;
  quantity: number;
  userEmail: string;
  productPicture: string;
  productName: string;
  productDescription: string;
  status: number;
  paymentCode: string;
}
